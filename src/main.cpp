#include <iostream>
#include <fstream>

void SplitTxtIntoWords(std::string filename)
{
    std::ifstream file(filename);
    std::string s;
    while (std::getline(file, s))
    {
        // ...
    }

}

int main() {
    for (int i = 0; i <10; ++i) {

    }
    return 0;
}
